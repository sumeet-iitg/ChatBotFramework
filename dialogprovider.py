from episodeformats import Episode

class DialogProvider(object):
    def __init__(self, platForm, sessionId):
        self.state = -1
        self.platFormId = platForm
        self.sessionId = sessionId

    def InitializeDialogProvider(self, episode):

    def InitializeDialogProvider(self, logFile, episode):

    def ProcessLogFile:

    def ComputeNextState